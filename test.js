"use strict";

function main() {
    let percent = prompt("На сколько процентов ты лох?");
    if (percent > 50) {
        alert("Ну ты и лох, тьфу!");
    } else {
        alert("Красава, лошара!");
    }
}